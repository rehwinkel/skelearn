import React from "react";

function RealExam({ textMode, imageMode, timed, category }: { category: string, textMode: boolean, imageMode: boolean, timed: boolean }) {
    return (<div>real</div>); //TODO
}

export default RealExam;