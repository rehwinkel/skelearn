import { useRoute } from "wouter";

function SpacedRepExam({ textMode, imageMode }: { textMode: boolean, imageMode: boolean }) {
    return (<div>hi spaxced rep</div>);
}

export default SpacedRepExam;